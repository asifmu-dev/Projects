# const message = document.getElementById("message");: Retrieves the HTML element with the ID "message" and assigns it to the variable message.

# const onlineMessage = "You are online now"; and const offlineMessage = "Oops! Internet is disconnected";: Declare and initialize variables for the online and offline message texts.

# const updateMessage = () => { ... }: Defines a function named updateMessage that updates the message based on the online status.

# updateMessage();: Calls the updateMessage function immediately to set the initial message.

# setInterval(updateMessage, 1000);: Sets up a timer to call the updateMessage function every 1 second (1000 milliseconds) to update the message continuously.
